\section{Outlook}
Notes on outlook:
\begin{itemize}
    \item action operads: Could we define a functorial definition for any group operad?
    \item functor $\pi$ gives equivalent definition of $\B$. Opening the door to the higher categorical context definition
\end{itemize}


\todo{algebras over colored braided operads ?
Expect: Given a colored braided operad B we can define algebras over B valued in a braided monoidal category.}


\todo{some version of}
\begin{defin}
    A braided $\infty${\em-operad} is a functor $p \colon \Bx \to \nerv(\Brx)$ between
    $\infty$-categories satisfying the following conditions:
    \begin{enumerate}[(i)]
        \item For every inert morphism $f \colon \langle n \rangle \to \langle m
        \rangle$ in $\nerv(\Brx)$ and object $C \in \Bx_{\langle m \rangle}$
        there exists a $p$-cocartesian morphism $f \colon C \to C'$ in $\Bx$
        lifting $f$. In particular $f$ induces a functor $f_{!} \colon
        \Bx_{\langle n \rangle} \to \Bx_{\langle m \rangle}$.
        \item Let $C \in \Bx_{\langle m \rangle}$ and $C' \in \Bx_{\langle n
        \rangle}$ be objects, $f \colon \langle m \rangle \to \langle n \rangle$ be a
        morphism in $\Brx$ and let $\Map_{\Bx}^f(C, C')$ be the union of
        connected components of $\Map_{\Bx}(C, C')$ which lie over $f$. Choose
        $p$-cocartesian morphisms $C' \to C'_i$ lying over the inert morphism
        $\rho^i \colon \langle n \rangle \to \langle 1 \rangle$ for $1 \leq i
        \leq n$. Then the induced map
        \[
            \Map_{\Bx}^f(C, C') \to \prod_{1 \leq i \leq n} \Map_{\Bx}^{\rho^i \circ f}(C, C_i')
        \]
        is a homotopy equivalence.
        \item For every finite collection of objects $C_1, \dots, C_n \in
        \Bx_{\langle 1 \rangle}$, there exists an object $C \in \Bx_{\langle n
        \rangle}$ and a collection of $p$-cocartesian morphism $C \to C_i$
        covering $\rho^i \colon \langle n \rangle \to \langle 1 \rangle$.
    \end{enumerate}
\end{defin}