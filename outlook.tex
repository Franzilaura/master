\section{Outlook}



An interesting next step would be to consider algebras over colored braided
operads. Similarily as we can define algebras over a colored symmetric operad
$\O$ valued in a symmetric monoidal category, we expect that we can define
algebras over a colored braided operad $\B$ valued in a braided monoidal
category.
\medskip

Eventually, we would like operadic fibrations to open the door to a higher categorical
notion of a braided $\infty$-operad which could be defined as follows.

\begin{defin} 
    A braided $\infty${\em-operad} is a functor $p \colon \Bx \to \nerv(\Brx)$
    between $\infty$-categories satisfying the following conditions:
    \begin{enumerate}[(i)]
        \item The functor $p$ is a categorical fibration by Definition \ref{def:cat-fib-infty}.
        In particular $f$ induces a functor $f_{!} \colon
        \Bx_{\langle n \rangle} \to \Bx_{\langle m \rangle}$.
        \item Let $C \in \Bx_{\langle m \rangle}$ and $C' \in \Bx_{\langle n
        \rangle}$ be objects, $f \colon \langle m \rangle \to \langle n \rangle$
        be a morphism in $\Brx$ and let $\Map_{\Bx}^f(C, C')$ be the union of
        connected components of $\Map_{\Bx}(C, C')$ which lie over $f$. Choose
        $p$-cocartesian morphisms $C' \to C'_i$ lying over the inert morphism
        $\rho^i \colon \langle n \rangle \to \langle 1 \rangle$ for $1 \leq i
        \leq n$. Then the induced map
        \[
            \Map_{\Bx}^f(C, C') \to \prod_{1 \leq i \leq n} \Map_{\Bx}^{\rho^i \circ f}(C, C_i')
        \]
        is a homotopy equivalence.
        \item For every finite collection of objects $C_1, \dots, C_n \in
        \Bx_{\langle 1 \rangle}$, there exists an object $C \in \Bx_{\langle n
        \rangle}$ and a collection of $p$-cocartesian morphism $C \to C_i$
        covering $\rho^i \colon \langle n \rangle \to \langle 1 \rangle$.
    \end{enumerate}
\end{defin}

Furthermore, we have seen the braid operad as an action operads in Section \ref{sec:colbraidop}, 
which are the first
step towards group operads following \cite{yau}. An intriguing question that remains open is
whether the framework developed here can be generalized to other group operads.
However, as our framework is very much tailored to the structure of braidings, we do not see a natural extension.